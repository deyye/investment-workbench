# 整合验证记录

日期：2026-09-16。Python 3.12。

## 实测结果

`python -m pytest tests -q`：**407 passed, 10 skipped, 22 subtests passed**，约 41 秒。

- 保留并运行审批与政策业务测试，覆盖字段、指标、阶段、证据、人工复核、去重、附件、分类、配置、任务和数据维护。
- 新增 9 个集成测试用例（含参数化）：首页及前缀路由、静态资源和政策站内链接；写操作来源及 CSRF；文本/视觉共用模型；配置热更新与关闭模型；配置文件重载、密钥不回显及换服务校验；异常 JSON、截断与拒绝输出；PDF 上传、后台任务、原文 PNG、修改、导出与磁盘重读；政策样例入库、幂等和带前缀复核跳转。
- 共用模型通过真实本地 HTTP 模拟服务验证，不使用真实 API 密钥或付费模型。
- Flask 测试客户端覆盖一个装配后的应用；另以 `python start.py --port 8765 --data-dir /tmp/workbench-browser-data` 实际启动服务，验证单进程启动。
- 原测试有 10 项因没有对应原始真实 PDF 材料而跳过；不算通过。

## 迁移时调整的测试

- 两套测试移入 `tests/approval` 和 `tests/policy`，修正相对路径及审批测试间的导入。
- 模型配置测试更新：切换服务必须提供新密钥；新增集成测试验证旧密钥不能被静默发送到新服务。
- 本地 HTTP 测试添加 `NO_PROXY=127.0.0.1,localhost`，避免环境代理误拦截回环请求。

## 尚未验证

- 当前远程浏览器访问本地 URL 被 `ERR_BLOCKED_BY_CLIENT` 拦截，未完成视觉截图或真实浏览器点击验收。
- 未实测真实外部模型服务、真实视觉识别质量及所有公网采集来源当前可用性。
- 当前环境无 Docker，Dockerfile / compose 已提供，未运行镜像构建。
- 未进行多人并发、压力、崩溃恢复及多进程部署验收。

历史验证记录在 `docs/upstream/`；这些历史成绩与本次测试结果分开理解。
